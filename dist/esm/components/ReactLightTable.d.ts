import Props from "./Models/Props";
declare const ReactLightTable: <T>({ id, columns, data, sortable, headerTextAlign, contentTextAlign, className, hasPagination, initSort, numberRows, rowsPerPage, loading, handleRowClick, rowKey, afterSort, onCurrentDataChange, }: Props<T>) => JSX.Element;
export default ReactLightTable;
